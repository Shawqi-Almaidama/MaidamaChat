cat > environment.yml <<EOL
name: maidamachat
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - setuptools
  - wheel
  - pip:
      - flask>=2.3.2
      - google-generativeai>=0.27.0
EOL
